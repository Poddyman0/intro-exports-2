// complete THESE require statements in order to log "hello there northcoders" to the console
// when running THIS file

const string1 = require('./data/source1.js'); //'hello'
const string2 = require('./data/source2.js'); // 'northcoders'
const string3 = require('./data/source3.js'); // 'there'

const greeting = `${string1} ${string3} ${string2}`;

console.log(greeting);
